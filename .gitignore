# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
.env
.env.*
*.egg-info/
.eggs/
dist/
build/

# 本机 DolphinDB 覆盖（复制 config/local.example.py 为 local.py）
config/local.py
# 若仍保留根目录 config.py 会与 config/ 包冲突，勿提交
config.py

# Local / cache
.ddb_ticker_cache/
*.log

# 导入断点（本机进度，一般不提交）
ingestion/massive_import_checkpoint.json

# Editor / IDE
.idea/
.vscode/
*.swp
*~

# Cursor / local tooling
.cursor/

# Bundled deps (if recreated locally)
.vendor/

# OS
.DS_Store
Thumbs.db
